// Dependencias.
import React, { useState, useEffect } from "react";

// Estilos
import "./Header.scss";

// Definición del componente: <Header />
const Header = (props) => {
  // 1. Manejo del estado.
  // 2. Ciclo de vida.
  // 3. Metodos.
  // 4. Render.
  return (
    <div>
      <h1>Header</h1>
    </div>
  );
};

// PropTypes.
Header.propTypes = {};

// Exportación.
export default Header;
