// Dependencias.
import React, { useState, useEffect } from "react";

// Estilos
import "./Card.scss";

// Definición del componente: <Card />
const Card = (props) => {
  // 1. Manejo del estado.
  // 2. Ciclo de vida.
  // 3. Metodos.
  // 4. Render.
  return (
    <div>
      <h1>Card</h1>
    </div>
  );
};

// PropTypes.
Card.propTypes = {};

// Exportación.
export default Card;
