// Dependencias.
import React, { useState, useEffect } from "react";
import PropTypes from "prop-types";

// Estilos.
import "./Modal.scss";

// Definición del componente: <Modal />
const Modal = (props) => {
  // 1. Manejo del estado.
  const {} = props;

  // 2. Ciclo de vida.
  useEffect(() => {}, []);

  // 3. Metodos.

  // 4. Render.
  return <h1>Modal</h1>;
};

// PropTypes
Modal.propTypes = {};

// Exportación.
export default Modal;
