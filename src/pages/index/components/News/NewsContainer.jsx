// Dependencias
import React, { useState, useEffect } from "react";
import PropTypes from "prop-types";

// Estilos.
import "./NewsContainer.scss";

// Definición del componente: <NewsContainer />
const NewsContainer = (props) => {
  // 1. Manejo de estados.
  const {} = props;

  // 2. Ciclo de vida.
  useEffect(() => {}, []);

  // 3. Metodos.
  // 4. Render.
  return (
    <div>
      <h1>NewsContainer</h1>
    </div>
  );
};

// PropTypes.
NewsContainer.propTypes = {};

// Exportación.
export default NewsContainer;
