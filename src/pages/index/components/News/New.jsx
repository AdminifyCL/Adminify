// Dependencias
import React, { useState, useEffect } from "react";
import PropTypes from "prop-types";

// Estilos.
import "./New.scss";

// Definición del componente: <New />
const New = (props) => {
  // 1. Manejo de estados.
  const {} = props;

  // 2. Ciclo de vida.
  useEffect(() => {}, []);

  // 3. Metodos.
  // 4. Render.
  return (
    <div>
      <h1>New</h1>
    </div>
  );
};

// PropTypes.
New.propTypes = {};

// Exportación.
export default New;
