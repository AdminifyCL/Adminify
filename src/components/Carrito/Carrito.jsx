// Dependencias.
import React from "react";

// Importación de estilos.
// Importación de componentes.

// Definición del componente: <Carrito />.
const Carrito = () => {
  return (
    <div>
      <h1>Carrito</h1>
    </div>
  );
};

// Exportación del componente: <Carrito />.
export default Carrito;
