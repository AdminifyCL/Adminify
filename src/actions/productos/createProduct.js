// Dependencias.

// Configuraciones.
import { actionProductTypes } from "../../types/actionProductTypes.js";

const createProduct = (data) => {
  console.log(`[🛂][ACTION][${actionProductTypes}]`);

  // Fetch
  return async (dispatch) => {
    try {
    } catch (error) {}
  };
};

// Exportación.
export default createProduct;
